// import { Set } from 'immutable';

// import { weakCache2 } from 'utils/weakCache';

// export const subtract = <M, T extends Set<M>>() =>
//   weakCache2<T, T, T>((a: T, b: T) => a.subtract(b));

// const x: Set<number> = Set([3]);
// const y: Set<number> = Set([4]);

// const z = subtract<number, Set<number>>()(x, y);
